apiVersion: v2
name: pve-exporter
description: Prometheus Proxmox VE exporter configured through the upstream Helm chart.
type: application
version: 0.6.0
appVersion: "3.9.0"
dependencies:
  - name: prometheus-pve-exporter
    version: 2.8.0
    repository: https://charts.christianhuth.de
