# Packaged Helm dependencies are reproducible from Chart.lock. The source
# charts in the repository root remain tracked.
**/charts/*.tgz

# Terraform/OpenTofu runtime files. Provider locks are intentionally tracked.
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
**/crash.log
**/crash.*.log
