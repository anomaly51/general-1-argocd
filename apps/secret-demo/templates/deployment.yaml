apiVersion: apps/v1
kind: Deployment
metadata:
  name: secret-demo
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: secret-demo
      app.kubernetes.io/instance: secret-demo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: secret-demo
        app.kubernetes.io/instance: secret-demo
    spec:
      automountServiceAccountToken: false
      containers:
        - name: app
          image: busybox:1.37.0
          command: ["httpd"]
          args: ["-f", "-p", "8080", "-h", "/www"]
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: secret
              mountPath: /www
              readOnly: true
      volumes:
        - name: secret
          secret:
            secretName: secret-demo
            items:
              - key: message
                path: index.html
