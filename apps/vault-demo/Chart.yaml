apiVersion: v2
name: vault-demo
description: Small teaching example for Vault Secrets Operator.
type: application
version: 0.3.0
appVersion: "1.37.0"
dependencies:
  - name: vault-secret
    alias: vaultSecrets
    version: 0.1.0
    repository: file://../../shared/vault-secret
