apiVersion: v2
name: vault-secrets-operator
description: Temporary source retained only for Argo CD PreDelete hooks.
type: application
version: 0.2.0
dependencies:
  - name: vault-secrets-operator
    version: 1.5.0
    repository: https://helm.releases.hashicorp.com
