apiVersion: v1
kind: Service
metadata:
  name: argocd-server-nodeport
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  ports:
    - name: https
      port: 443
      targetPort: 8080
      nodePort: {{ .Values.argocd.nodePort }}
  selector:
    app.kubernetes.io/name: argocd-server
